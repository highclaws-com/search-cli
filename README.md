# search-engine-cli

A local command-line interface (CLI) tool to interact with and test Search Engine REST APIs.

## Installation

1. Clone the repository or navigate to the project directory.
2. Install the dependencies:

```bash
cd src
npm install
```
